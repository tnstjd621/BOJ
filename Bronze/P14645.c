// P14645 와이버스 부릉부릉 Bronze V

#include <stdio.h>

int main() {
    int n,m;
    scanf("%d %d", &n, &m);
    for(int i = 0; i<n; i++){
        int x, y;
        scanf("%d %d", &x, &y);
    }
    printf("비와이");
    return 0;
}